# Review v0.0.3

## Scope

- restore legacy-style submit entrypoints under `submitter/`
- align preview/render behavior with the old monolith
- add submit interface compatibility tests

## Findings

- No blocking findings.
- Legacy-compatible wrapper names now exist:
  - `submit_powershell.py`
  - `submit_powershell_ssh.py`
  - `submit_gitbash.py`
  - `submit_gitbash_ssh.py`
  - `submit_files.py`
- Residual risk: these wrappers currently preserve CLI and preview shape, but still target the new forward/backward protocol underneath.

## Decision

Release `v0.0.3`.
