# Evaluation v0.0.8

## Coverage

- active-process executor lifecycle
- stale finalization and detached cleanup path
- submitter legacy-style CLI output and timeout wording
- executor legacy-style polling/logging behavior
- real local submitter/executor roundtrip

## Commands

- `python3 -m unittest tests.test_executor_flow tests.test_submitter_interfaces tests.test_cli_entrypoints tests.test_integration_repos -v`

## Result

- executor now:
  - pushes ACK durably before launch
  - starts tasks into an in-memory active set
  - polls active tasks from later scan passes
  - writes `stale` on deadline expiry
- submitter now:
  - prints `SUBMITTED command_id=...`
  - replays stdout/stderr tails
  - uses legacy-style timeout wording
- focused regression suite passed
