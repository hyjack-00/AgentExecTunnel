# Evaluation v0.0.5

## Coverage

- repository-local default settings
- bootstrap localization of broken local file remotes
- `.gitmodules` HTTPS origin declarations
- fresh-clone bootstrap path
- local integration with separate submitter/executor clones against the same remotes
- documentation pass for repository-operation sequencing

## Commands

- `python3 -m unittest tests.test_cli_entrypoints tests.test_fresh_clone tests.test_integration_repos -v`
- `git config -f .gitmodules -l`

## Result

- repository-local defaults now resolve to:
  - `agent_forward/`
  - `agent_backward/`
- bootstrap now reports and repairs local file-based submodule origins into `var/local_remotes/`
- `.gitmodules` now declares:
  - `https://github.com/hyjack-00/agent_forward.git`
  - `https://github.com/hyjack-00/agent_backward.git`
- focused validation passed
- design docs now include explicit git-operation sequence diagrams for:
  - supported separate submitter/executor clones
  - unsupported shared submodule worktrees
