# Evaluation v0.0.6

## Coverage

- executor task-timeout finalization
- executor retry wrapper behavior for transient scan failures
- fresh-clone bootstrap path
- real local submitter/executor roundtrip with separate clones
- documentation pass for continuously-running executor semantics

## Commands

- `python3 -m unittest tests.test_executor_flow tests.test_cli_entrypoints tests.test_fresh_clone tests.test_integration_repos -v`

## Result

- executor now retries transient sync/push failures instead of exiting the long-running loop
- executor now converts subprocess timeout into a durable `stale` result with timeout detail
- repository-local bootstrap and separate-clone roundtrip coverage still pass
- design/docs now explicitly describe the continuously-running executor model
